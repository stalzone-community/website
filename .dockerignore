node_modules
.svelte-kit
build
.git
.github

# The upstream database is cloned fresh inside the image build — shipping the
# local 220 MB copy into the build context would be pure upload cost.
vendor

# Generated by scripts/build-items.ts during the image build.
src/lib/data/items.json
src/lib/data/search-index.json
static/icons

.env
.env.*
!.env.example
src/lib/data/recipes.json
