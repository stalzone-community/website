engine-strict=true
# npm 12 refuses git dependencies unless this is set, and accepts only
# all | none | root. `root` would cover uar-shared but not the
# sveltekit-commons it depends on in turn, so it has to be `all`.
# (The old `allow-git=github:Geptyro/*` form is invalid here and was ignored.)
allow-git=all
